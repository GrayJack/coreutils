name: Rust

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macOS-latest]
        rust: [1.37.0, stable, beta, nightly]
        components: [rust-std-x86_64-unknown-netbsd rust-std-x86_64-unknown-freebsd]
    steps:
    - uses: actions/checkout@v1
    - uses: hecrj/setup-rust-action@v1
      with:
        rust-version: ${{ matrix.rust }}
        components: ${{ matrix.components }}
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
    - name: Check FreeBSD
      run: cargo check --verbose --target x86_64-unknown-freebsd
    - name: Check NetBSD
      run: cargo check --verbose --all --target x86_64-unknown-netbsd
